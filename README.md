# FlyTire

## Admin login

- Default admin login: `admin`
- Default admin password: `flytire-admin`

You can override these in `backend/.env`:

```env
ADMIN_LOGIN=your_login
ADMIN_PASSWORD=your_password
```
